import { Alert } from "react-bootstrap"

const Error = () => {
    return (
        <Alert variant="danger" className="text-center text-uppercase fw-bolder">
            Página no encontrada
        </Alert>
    )
}

export default Error